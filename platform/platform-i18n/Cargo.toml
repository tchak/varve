[package]
name = "platform-i18n"
version.workspace = true
edition.workspace = true
publish.workspace = true
license.workspace = true

# The MF2 catalogs (English + French) and their runtime over
# ICU4X (PLATFORM.md P.3). Locale is a plain argument resolved in
# `platform-app`; no crate below this one knows what a locale is. The
# runtime choice is gated on the P.9 Q8 spike.
[dependencies]
